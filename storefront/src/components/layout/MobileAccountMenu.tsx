"use client";

/**
 * @deprecated این مؤلفه با `MobileDrawerMenu` جایگزین شده است (همبرگر + drawer
 * تمام‌قد از سمت راست). برای جلوگیری از کد موازی، صرفاً مؤلفه‌ی جدید را
 * re-export می‌کند. این فایل دیگر در Header استفاده نمی‌شود و می‌توان آن را حذف
 * کرد.
 */

export { MobileDrawerMenu as MobileAccountMenu } from "@/components/layout/MobileDrawerMenu";
